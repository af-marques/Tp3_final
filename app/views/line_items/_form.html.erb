<%= form_for(@line_item) do |f| %>
  <% if @line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_item.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
      <% @line_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <% @bla = [] %>
    <% Product.all.each do |pro| %>
        <% @bla.append [pro.name ,pro.id] %>
    <%end%>

    <div class="field">
      <%= f.label :product_id %><br />
      <%= f.select :product_id, @bla%>
  </div>

    <% @bla = [] %>
    <% Order.all.each do |o| %>
        <% @bla.append [o.id ,o.id] %>
    <%end%>

    <div class="field">
    <%= f.label :order_id %><br />
    <%= f.select :order_id , @bla%>


      <div class="field">
        <%= f.label :quantity %> <br/>
        <%= f.text_field :quantity %>
      </div>

    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
